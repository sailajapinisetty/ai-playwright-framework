# Multi-Agent Dashboard - Story 1 (UI input)

Generated at: 2026-05-25T20:07:44.489Z
Overall status: PARTIAL_FAIL

| Case ID | Attempt | Execution | UI Validation | Priority | Script |
| --- | --- | --- | --- | --- | --- |
| APPSTORE_Story_1_TestCase_1 | 1 | FAIL | FAIL | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_1/appstore_story_1_verify_product_list_is_displayed_on.spec.js |
| APPSTORE_Story_1_TestCase_1 | 2 | FAIL | FAIL | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_1/appstore_story_1_verify_product_list_is_displayed_on.spec.js |
| APPSTORE_Story_1_TestCase_2 | 1 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_2/appstore_story_1_verify_each_product_displays_a_product.spec.js |
| APPSTORE_Story_1_TestCase_2 | 2 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_2/appstore_story_1_verify_each_product_displays_a_product.spec.js |
| APPSTORE_Story_1_TestCase_3 | 1 | PASS | FAIL | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_3/appstore_story_1_verify_each_product_displays_a_loaded.spec.js |
| APPSTORE_Story_1_TestCase_4 | 1 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_4/appstore_story_1_verify_each_product_displays_a_price.spec.js |
| APPSTORE_Story_1_TestCase_4 | 2 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_4/appstore_story_1_verify_each_product_displays_a_price.spec.js |
| APPSTORE_Story_1_TestCase_5 | 1 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_5/appstore_story_1_verify_product_list_loads_within_acceptable.spec.js |
| APPSTORE_Story_1_TestCase_5 | 2 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_5/appstore_story_1_verify_product_list_loads_within_acceptable.spec.js |
| APPSTORE_Story_1_TestCase_6 | 1 | FAIL | FAIL | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_6/appstore_story_1_verify_empty_state_message_when_no.spec.js |
| APPSTORE_Story_1_TestCase_6 | 2 | PASS | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_6/appstore_story_1_verify_empty_state_message_when_no.spec.js |
| APPSTORE_Story_1_TestCase_7 | 1 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_7/appstore_story_1_verify_product_list_is_displayed_correctly.spec.js |
| APPSTORE_Story_1_TestCase_7 | 2 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_7/appstore_story_1_verify_product_list_is_displayed_correctly.spec.js |
| APPSTORE_Story_1_TestCase_8 | 1 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_8/appstore_story_1_verify_product_list_is_displayed_correctly.spec.js |
| APPSTORE_Story_1_TestCase_8 | 2 | FAIL | PASS | high | generated_tests/APPSTORE_Story_1/test-cases/APPSTORE_Story_1_TestCase_8/appstore_story_1_verify_product_list_is_displayed_correctly.spec.js |

## Summary
Total automated cases: 15 | Execution passed: 2 | Execution failed: 13

## Improvement Suggestions

### APPSTORE_Story_1_TestCase_1
- Replace the single rigid `[data-testid='product-list']` selector with a prioritized fallback selector chain that includes semantic and structural alternatives: `[data-testid='product-list']`, `.product-list`, `#product-list`, `[class*='product-list']`, `[class*='ProductList']`, `ul.products`, `.products-grid`, `[class*='product-grid']`, `main ul`, `main .card`, so that `firstExisting()` can resolve even when data-testid attributes are absent
- Add a DOM introspection step before the assertion: after page load, log `page.locator('body').innerHTML()` (truncated) or use `page.evaluate(() => document.body.outerHTML.slice(0,3000))` to capture actual rendered markup into the test report, enabling selector diagnosis without relying on screenshots
- Instrument the test with `await page.waitForLoadState('networkidle')` and an explicit `await page.waitForSelector('main', { timeout: 10000 })` before attempting product list resolution, ensuring the page has fully rendered dynamic/React content before any assertions run
- Add a product-count assertion after the container resolves: assert `await page.locator('[data-testid="product-list"] > *, .product-list > *, [class*="product"]').count()` is greater than 0, ensuring the list is not only visible but actually populated with at least one item
- Compress or skip attaching full-page screenshots to the validator agent; instead crop the screenshot to the main content area (e.g., clip: { x:0, y:0, width:1280, height:800 }) before attaching, to stay within the 5 MB image limit and prevent validator agent failures caused by oversized base64 payloads
- Introduce a lightweight page-inspection utility that runs `page.$$eval('*[data-testid]', els => els.map(e => e.dataset.testid))` on failure and appends the discovered data-testid values to the error context markdown, so the next automation iteration can use verified attribute values instead of guessing
- Parameterize the base URL and ensure `await page.goto(BASE_URL, { waitUntil: 'domcontentloaded' })` is followed by an explicit wait for a stable landmark (e.g., `header` or `nav`) before proceeding, catching navigation or deployment issues early with a clear error message distinct from selector failures

### APPSTORE_Story_1_TestCase_1
- Replace the brittle `.card` CSS selector with a broader candidate selector list in the `resolveLocator` fallback chain — add alternatives such as `[data-testid='product-card']`, `[class*='product']`, `[class*='item']`, `.product-item`, `.product-list > *`, `li.product`, and `article` so the framework can self-heal when the actual DOM uses a different class name than `.card`
- Instrument the test to perform a DOM snapshot (page.content() or page.evaluate) after navigation and log all top-level container class names to stdout before the assertion step, enabling faster diagnosis of the real selector without needing a trace viewer
- Add an explicit `waitForLoadState('networkidle')` or `waitForSelector` with a generous timeout (e.g., 15 s) before attempting to resolve the product list locator, ruling out timing/race conditions as a contributing factor on slower CI environments
- Reduce screenshot file size before sending to the validator: compress the PNG (e.g., use `page.screenshot({ type: 'jpeg', quality: 60 })`) or clip to the viewport center to stay under the 5 MB API limit and unblock automated visual validation
- Introduce a data-testid attribute (`data-testid='product-card'`) on the product card component in the application source and update the selector strategy to target it as the primary locator, making tests resilient to CSS refactoring
- Add a smoke-level pre-check that asserts the API endpoint (e.g., GET /api/products) returns HTTP 200 and a non-empty JSON array before the UI assertion; if the API fails, the test should be marked as blocked rather than failed to distinguish app bugs from selector issues
- Create a shared `ProductListPage` POM class that encapsulates all product-list selectors and exposes a `waitForProducts()` method, preventing selector duplication across future test cases for this story
- Capture a full-page screenshot at a reduced resolution or as a clipped region (top 800 px) instead of the full page to ensure validator image payloads stay within the 5 MB threshold on all viewport sizes

### APPSTORE_Story_1_TestCase_2
- Replace the brittle `.product-list` CSS class selector with a resilient multi-candidate selector array that includes semantic and structural alternatives such as `[data-testid='product-list']`, `ul.products`, `div.product-grid`, `section.products`, and a role-based fallback like `role=list` scoped within the main content area — matching the actual grid layout confirmed by the UI validation screenshot.
- Add a DOM introspection step at test startup that logs all top-level container class names and data attributes present on the landing page, enabling automatic selector discovery when the primary selector fails instead of throwing immediately.
- Update `basePage.js firstExisting()` to accept an expanded candidate list with at least 5 fallback selectors for any product-list container assertion, ordering them from most specific (data-testid) to most generic (CSS grid/flex containers holding card children).
- Introduce a `waitForProductCards()` helper that waits for at least one product card element (e.g., `[data-testid='product-card']`, `.product-card`, `.card`, `article`) to be visible before asserting the parent container, preventing premature resolution failures on slow renders.
- Add a dedicated selector mapping configuration file (e.g., `selectors.json`) for the landing page product list so selector updates are centralized and do not require script regeneration — the UI validation confirmed the grid renders 6 product cards reliably, making a stable selector registry feasible.
- Extend the test to iterate over all resolved product card elements using `page.locator('...').all()` and assert `innerText` is non-empty for each, directly implementing the manual step 'inspect each product card and verify a non-empty product name' — this assertion is currently missing from the failing script.
- Capture a DOM snapshot (HTML of the main content area) as a named attachment on selector resolution failure, supplementing the existing screenshot, to accelerate debugging of which actual class names or attributes are present at failure time.

### APPSTORE_Story_1_TestCase_2
- Replace the hardcoded '.card' selector with a broader candidate list in the resolveLocator call: try '[class*="card"]', '[data-testid*="product"]', '.product-item', 'article', and '[class*="product"]' as fallback candidates so the framework's firstExisting() resolves the actual DOM structure instead of failing on a single class name
- Add a pre-assertion page-load guard using 'await page.waitForLoadState("networkidle")' and 'await page.waitForSelector("h2, [class*=card], article", { timeout: 10000 })' before any product card locator resolution to ensure the product grid is fully rendered before assertions run
- Update the product card locator strategy to use a semantic or data-attribute selector such as '[data-testid="product-card"]' or the actual rendered element structure (e.g., '.product-grid > *' or 'ul.products > li') discovered from a DOM inspection of the live page, since screenshots confirm 6 cards render correctly but the '.card' class does not match the actual class names in the DOM
- Introduce a locator discovery step in the test scaffold that logs all candidate matches for product-like elements (article, section > div, [class*=card], [class*=product]) when no primary selector resolves, enabling faster diagnosis in future failures without requiring trace inspection
- Refactor the product name assertion to iterate over resolved product card locators and check 'innerText' of the child heading element using a relative locator (e.g., locator.locator('h2, h3, [class*=name], [class*=title]').first()) so the test remains resilient to heading level changes
- Add a minimum product count assertion (expect(productCards.count()).toBeGreaterThanOrEqual(3)) after resolving the product list to formally enforce the 'multiple products' acceptance criterion that the manual case and validation both confirm is met
- Capture the actual DOM class names of product cards during a successful navigation using 'page.evaluate(() => [...document.querySelectorAll("main *")].map(el => el.className))' in a debug/discovery test run, then hardcode the confirmed selector into the POM to eliminate guesswork across all product-related test cases

### APPSTORE_Story_1_TestCase_3
- Fix the naturalWidth check to correctly fail when any image is broken: ensure the evaluate() call iterates ALL img elements within product cards and asserts naturalWidth > 0 for each one individually, then surfaces which product's image failed (e.g., 'Arc Mechanical Keyboard') in the error message instead of silently passing.
- Add an explicit assertion that counts broken images using page.evaluate(() => [...document.querySelectorAll('.product-card img, [data-testid*="product"] img')].filter(img => img.naturalWidth === 0 || img.naturalHeight === 0)) and assert the result array is empty, failing the test with the list of broken product names.
- Add a network response listener (page.on('response', ...)) to capture failed image requests (status 404, 0, or other non-2xx) for all src URLs of product images, and fail the test if any image URL returns a non-successful HTTP status — this catches broken images before DOM evaluation.
- Scope the product image selector more precisely to avoid missing any product card: use a robust selector like 'article img, .product-card img, [class*="product"] img' and assert the count matches the expected total number of products displayed (visible product count should equal loaded image count).
- Add a waitForFunction step after page load to wait until ALL product images have either loaded or errored (using Promise.all on image.decode() or checking complete && naturalWidth > 0) before running assertions, preventing race conditions where images appear to pass before the browser finishes loading them.
- Add a screenshot-on-failure hook that captures and annotates which specific product card contains the broken image, attaching it as a test artifact to make debugging faster when validation catches UI regressions the script misses.
- Extend the test to run on Firefox in addition to Chromium and WebKit, since broken image behavior and error states can differ across browser engines and the current 2-browser setup missed the regression.

### APPSTORE_Story_1_TestCase_4
- Replace the single '.product-list' CSS class selector with a multi-candidate fallback array in the resolveLocator/firstExisting logic, adding alternative selectors such as '[data-testid="product-list"]', '.product-grid', '.products-container', 'ul.products', 'section.products', and '[class*="product"]' so the framework can resolve the container even when the exact class name differs from assumptions
- Add a DOM inspection step (page.evaluate or page.content()) in the test setup to log the actual CSS classes and data attributes of the product list container before attempting assertions, enabling faster debugging when selectors mismatch
- Introduce a page.waitForSelector with a broad semantic selector (e.g., '[class*="product"]') and a reasonable timeout (10000ms) before the resolveLocator call to ensure the product grid has mounted, especially in webkit which may render slower
- Enumerate all product card price elements using page.locator('[class*="price"]') or page.locator('[class*="product"] [class*="price"]').all() and assert each matches /^\$[0-9]+(\.[0-9]{2})?$/ via a loop, replacing any hard-coded single-element assertion
- Add a precondition assertion that the product count is greater than zero (expect(cards.length).toBeGreaterThan(0)) before iterating price checks so the test fails with a clear message if the grid is empty rather than silently passing
- Extract product list container selector candidates into a shared POM constants file (e.g., PRODUCT_LIST_SELECTORS) so all test cases targeting the product grid use a single maintained source of truth and updates propagate automatically
- Capture a full-page screenshot immediately after navigation and before any assertion in the test script (not only on failure) to provide a baseline visual artifact for debugging selector mismatches across browsers
- Since visual validation already confirms the feature works correctly in the real UI (confidence 97%), mark this test as a known-flaky selector issue and add a GitHub/Jira tracking comment in the spec file so it is prioritised for selector fix in the next sprint rather than being re-run blindly

### APPSTORE_Story_1_TestCase_4
- Replace the hardcoded '.card' selector with a broader candidate list in the resolveLocator call. Expand the selector candidates array to include common product card patterns such as '[data-testid*="product"]', '[class*="product"]', '[class*="item"]', '[class*="card"]', 'article', '.product-card', '.product-item', '[data-testid="product-card"]' so the framework's firstExisting resolver can match the actual DOM structure used by this application.
- Inspect the live DOM (via Playwright page.content() or debug screenshot) immediately after navigation to identify the exact CSS class or data-testid attribute used for product cards in this app, then hardcode that confirmed selector as the primary candidate (e.g., '[data-testid="product-card"]' or '.product-grid .item') to eliminate resolver ambiguity on future runs.
- Add a dedicated page-load wait step before the resolveLocator call: use 'await page.waitForLoadState("networkidle")' followed by 'await page.waitForSelector("known-product-selector", { timeout: 10000 })' to ensure the product grid has fully rendered before any assertions are attempted, preventing premature selector resolution failures.
- Update the resolveLocator / firstExisting utility to log all attempted selectors and their match counts when resolution fails, rather than throwing immediately, so engineers can diagnose which selectors were tried and why none matched without requiring a full re-run with manual inspection.
- Since the UI validation (confidence 97%) confirmed all 6 product cards render with correctly formatted '$' prices, add a Playwright assertion that iterates over all resolved product card locators and for each verifies an inner text match against the regex /\$\d+(\.\d{2})?/ — ensuring price format coverage scales automatically if the product count changes.
- Add a precondition guard assertion at the start of the test: 'await expect(page).toHaveURL(/landing|home|\/$/)' to verify the correct page loaded before attempting product card assertions, enabling faster triage if navigation itself fails.
- Parameterise the expected product count (currently 6) as a configurable test fixture or environment variable so the test can assert 'expect(productCards).toHaveCount(expectedCount)' and fail explicitly on count mismatch rather than silently passing with a subset of cards.

### APPSTORE_Story_1_TestCase_5
- Add fallback selectors in the resolveLocator candidates array for the product list container beyond '[data-testid='product-list']', including semantic alternatives such as '.product-grid', '.product-list', '[class*="product"]', 'main ul', 'main .grid', and 'section[class*="product"]' to handle cases where data-testid attributes are not present in the DOM
- Audit the actual rendered HTML of the landing page (visible in validation screenshot as a product grid) to identify the real CSS class or element structure used, then hardcode that selector as the primary candidate in the locator resolution chain for product list detection
- Implement a DOM inspection step at test startup that logs all top-level container attributes (id, class, data-testid) within the main content area, enabling faster diagnosis when locator resolution fails in future runs
- Add a page.waitForLoadState('networkidle') call before attempting to resolve the product list locator to ensure the DOM is fully settled before selector evaluation, reducing race-condition-related failures
- Introduce a performance timing assertion using page.evaluate() to capture window.performance.timing.domContentLoadedEventEnd minus navigationStart, asserting the delta is less than 3000ms, replacing the sole reliance on element visibility as a proxy for load time
- Since the UI validation confirms the product list renders correctly (confidence 97), treat this as a selector-mismatch failure rather than a functional regression and prevent false-negative blocking of the pipeline by adding a known-selector smoke check before the full test runs
- Extend the firstExisting() fallback mechanism in basePage.js to support role-based locators such as page.getByRole('list') and page.getByRole('img', {name: /product/i}) as last-resort candidates when all CSS/data-testid selectors fail

### APPSTORE_Story_1_TestCase_5
- Replace the '.app' selector in AppPage.waitForVisible() with a more resilient product-list selector. The resolveLocator candidate array in appPage.js should include '.card', '[data-testid="product-card"]', '.product-list', and 'main' as fallback candidates so at least one resolves when the React tree mounts.
- In basePage.js firstExisting(), add a short explicit wait (e.g., page.waitForSelector with timeout 5000ms) for each candidate before discarding it, instead of immediately returning null — this prevents false 'unable to resolve' failures during React hydration delays.
- Remove or conditionally skip the '.app' root-container wait that triggers networkidle; the app does not appear to render a top-level element with class 'app'. Use page.waitForLoadState('domcontentloaded') followed by page.waitForSelector('.card', { state: 'visible' }) as the authoritative readiness signal.
- Add a DOM inspection step (page.evaluate(() => document.body.innerHTML.substring(0,500))) in the catch block of resolveLocator to log actual rendered markup on failure, making future selector mismatches faster to diagnose.
- Capture performance timing inside the test using page.evaluate(() => JSON.stringify(window.performance.timing)) after '.card' is visible, then assert (domContentLoadedEventEnd - navigationStart) < 3000 to fulfil AC4 explicitly rather than relying solely on implicit load timing.
- Extend the selector candidate list in appPage.js to cover both class-based ('.card') and attribute-based ('[class*="card"]', '[class*="product"]') selectors to guard against future CSS-module class-name changes.
- Add a Playwright test for webkit specifically using page.setExtraHTTPHeaders or a slower navigation timeout (10 s) since webkit often has slower initial paint in CI; this prevents webkit-only flakiness from masking real failures.
- Mark the test with test.slow() in Playwright for webkit to automatically triple the default timeout, preventing premature timeout failures on slower CI webkit runners without changing the 3-second product-visibility assertion.

### APPSTORE_Story_1_TestCase_6
- Intercept the products API endpoint using Playwright's route interception (page.route) to mock an empty array response before navigating to the landing page, ensuring the empty state condition is reliably triggered without requiring backend coordination or a special test environment
- Replace the single hard-coded selector '[data-testid="empty-state"]' with a broader fallback selector chain that includes text-based matchers such as 'text=/no products/i', 'text=/coming soon/i', 'text=/no items/i', and '.empty-state', since the application currently lacks a data-testid attribute for this element
- Add a pre-assertion step that verifies the product grid or product cards are NOT present (e.g., assert count of '[data-testid="product-card"]' equals 0) before checking for the empty state element, providing a clearer failure diagnostic if the API mock did not apply correctly
- Implement the API mock setup in a beforeEach hook using page.route('**/api/products*', route => route.fulfill({ status: 200, body: JSON.stringify([]) })) to guarantee empty state is rendered consistently across all browsers (chromium, webkit) without relying on actual backend state
- Add a DOM inspection step after navigation that logs all visible text content in the main content area when the empty state locator fails, enabling faster identification of what element or text the application actually renders in the empty scenario
- Update the test precondition handling to explicitly validate that the route mock was registered before page.goto() is called, and add a page.waitForResponse or page.waitForSelector timeout of at least 5000ms to account for async rendering of the empty state UI
- Expand the locator candidate list in the resolveLocator/firstExisting utility to automatically include semantic role-based selectors such as role=status, role=alert, and aria-label matchers for empty state scenarios as a general framework improvement for edge case tests

### APPSTORE_Story_1_TestCase_6
- Intercept the products API endpoint using Playwright's `page.route()` to return an empty array `[]` before navigating to the landing page, ensuring the empty state path is actually exercised rather than relying on the live environment having no products
- Add an explicit assertion that checks for a visible empty state element by text or test-id (e.g., `await expect(page.getByText(/no products available|coming soon/i)).toBeVisible()`) to confirm the empty state message renders correctly
- Add a negative assertion confirming that no product cards are rendered when the mock returns an empty array (e.g., `await expect(page.locator('[data-testid="product-card"]')).toHaveCount(0)`), preventing false positives if the mock route fails silently
- Capture a screenshot after the route mock is applied and page has loaded, then attach it as a test artifact so UI validation can confirm the empty state UI rather than returning UNKNOWN confidence
- Add a console error listener (`page.on('console', ...)`) at test setup to capture and assert no JavaScript errors occur during the empty-state render, addressing the UNKNOWN check on console errors
- Assert that the route mock was actually called (e.g., using `waitForResponse` or a request count check) to guarantee the interception succeeded before making empty-state assertions, preventing silent mock failures from causing false passes
- Run the test across all configured browsers (currently only chromium and webkit are listed; add firefox) to ensure cross-browser empty state rendering consistency
- Add a teardown step that removes/aborts the route mock after the test to prevent route leakage into subsequent tests in the same worker

### APPSTORE_Story_1_TestCase_7
- Replace the single rigid `[data-testid='product-list']` selector with a resilient fallback selector chain in the resolveLocator candidates array, e.g.: `[data-testid='product-list']`, `.product-list`, `.product-grid`, `[class*='product-list']`, `[class*='product-grid']`, `main ul`, `main .grid` — so the framework auto-resolves whichever is present in the DOM
- Inspect the actual rendered DOM via page.content() or a pre-failure debug step to identify the real container selector used by the app, then update the primary selector accordingly and optionally add `data-testid='product-list'` to the application component for future-proofing
- Add a DOM introspection step before the first assertion: use `page.evaluate()` to log all candidate container elements matching common product list patterns, capturing this in the error-context artifact to speed up selector diagnosis on failure
- Add a dedicated image load validation step using `page.$$eval('img')` to assert `naturalWidth > 0` for each product image, since the UI validation detected 5 out of 6 product images failed to render — this is a real defect that the automated test should catch and currently does not
- Separate the product image broken-render check into its own explicit assertion block with a soft assertion (`expect.soft`) so the test can continue verifying layout while still flagging image load failures, preventing a single image issue from masking other layout regressions
- Add an explicit `waitForLoadState('networkidle')` or `waitForSelector` on the product grid container after navigation to ensure all product data and images have had time to load before assertions run, reducing flakiness from race conditions
- Parameterize the viewport configuration to run the same test across multiple resolutions (1920x1080, 1440x900, 1280x800) using Playwright's `test.use({ viewport })` within a describe block, increasing coverage per the manual case intent
- Add a product count assertion using `page.locator('[data-testid="product-card"], .product-card, [class*="product-card"]').count()` to verify that the expected minimum number of products (e.g., >= 1) are rendered, strengthening grid population coverage

### APPSTORE_Story_1_TestCase_7
- Replace the hardcoded '.card' selector with a broader candidate list in the resolveLocator call. Update the selector candidates array to include '[data-testid="product-card"]', '.product-card', '.product-item', '[class*="card"]', '[class*="product"]', 'article', and '.grid > div' so the framework's firstExisting resolver can match the actual DOM structure rendered by the app.
- Add a page.waitForLoadState('networkidle') call immediately after navigation and before any product card assertions to ensure async product data fetched from the backend is fully rendered before selector resolution is attempted.
- Inspect the actual DOM in the failure screenshot/trace to identify the real CSS class or data attribute used for product cards, then hardcode that confirmed selector as the primary candidate (e.g., '[class*="product"]' or '[data-testid="product-card"]') at the top of the candidates list to avoid resolution failures on retry.
- Add a page.waitForSelector with a timeout of at least 10000ms targeting a broad fallback selector like 'main [class*="card"], main article, main [class*="product"]' before calling expectVisible, so the test explicitly waits for at least one product element to appear rather than immediately failing on an empty DOM.
- Augment the test with a DOM introspection step that logs page.$eval('body', el => el.innerHTML.substring(0, 2000)) on failure to capture the actual rendered markup, enabling faster diagnosis of selector mismatches without needing to manually open the trace file.
- Since UI validation passed with 95% confidence confirming products are visible, add a post-navigation assertion count check using page.locator('[class*="card"], article').count() and assert count >= 1 as a resilient numeric guard instead of relying solely on a single-class selector match.
- Parameterize the viewport configuration (1920x1080) via Playwright project config rather than inline in the test, and add a second project entry for 1280x800 to extend desktop coverage without duplicating test code.

### APPSTORE_Story_1_TestCase_8
- Expand the selector candidate list in the resolveLocator / firstExisting fallback chain to include semantic and structural alternatives for the product list container (e.g., '[data-testid="product-list"]', '[data-testid="products-grid"]', '[data-testid="product-grid"]', '.product-list', '.product-grid', 'ul.products', 'section.products', '[role="list"]', 'main .grid', 'main ul') so that the locator resolves even when data-testid attributes are absent or named differently
- Before asserting '[data-testid="product-list"]', add a DOM introspection step that logs all data-testid attribute values present on the page at the moment of failure; use page.evaluate(() => [...document.querySelectorAll('[data-testid]')].map(el => el.dataset.testid)) and attach the result to the error context so the correct selector can be identified without manual trace inspection
- Add a page.waitForLoadState('networkidle') and an explicit wait for at least one product card (e.g., page.locator('.product-card, [data-testid*="product"]').first().waitFor({ state: 'visible', timeout: 15000 })) before asserting the container, preventing race-condition failures where the product list has not yet been injected into the DOM at tablet viewport
- Set the tablet viewport (768x1024) via Playwright's built-in use: { viewport: { width: 768, height: 1024 } } in the project config or via page.setViewportSize at the top of the test, and verify the viewport is applied with page.viewportSize() before proceeding, ensuring the responsive layout is actually triggered
- Since the UI validation screenshot confirms all product cards, images, names, and prices are visible and correctly laid out, add assertions on the individual product cards directly (e.g., expect(page.locator('.product-card').first()).toBeVisible()) as a resilient fallback so the test passes even if the wrapper container selector is missing a data-testid
- Introduce a cross-browser selector normalisation helper that accepts a primary selector and an ordered list of fallbacks, iterates through them with a short per-selector timeout (e.g., 2 s), and resolves to the first match; this prevents both webkit and chromium from failing identically on the same missing data-testid
- Add a test setup assertion (beforeEach guard) that confirms the API returns at least one product (via page.waitForResponse matching the products endpoint) before any UI assertions are made, preventing false negatives caused by an empty or slow backend response at the tablet breakpoint

### APPSTORE_Story_1_TestCase_8
- Expand the selector candidate list in the resolveLocator/firstExisting logic to include Bootstrap and common card alternatives: ['.card', '[class*="card"]', '.product-card', '.product-item', '[data-testid="product-card"]', '.col .card', '.card-body'] so that if '.card' exists in the DOM but is nested differently it is still matched
- Add a networkidle or domcontentloaded wait after page.setViewportSize(768, 1024) and page.goto() before attempting any locator resolution, ensuring the product grid has fully rendered at the tablet viewport before assertions fire
- Before calling expectVisible('.card'), inject a page.locator('.card').first().waitFor({ state: 'visible', timeout: 10000 }) step so transient load delays do not cause immediate locator resolution failure
- Capture a DOM snapshot (page.content()) on failure and attach it as a test artifact alongside the screenshot, enabling offline inspection of actual class names used by the product cards at the time of failure
- In the test script, add a fallback assertion path: if '.card' is not resolved within 5 s, query the page for all visible elements containing product price patterns (e.g., /\$\d+/) and assert count > 0, providing a resilient secondary check aligned with the validation result that confirmed prices are visible
- Add an explicit viewport assertion early in the test (expect(page.viewportSize()).toEqual({ width: 768, height: 1024 })) to confirm the viewport was applied correctly before product list checks begin
- Introduce a smoke-level pre-check that scrolls the page once and verifies at least N=6 product cards are present (matching the 6 products confirmed by UI validation), replacing the current single-card visibility check with a count-based assertion: expect(await page.locator('.card').count()).toBeGreaterThanOrEqual(1)
- Flag and add a dedicated test for the two product images (Nova Desk Lamp, Terra Thermal Bottle) identified as missing/not loaded in the validation result, asserting img src is non-empty and HTTP response is 200 for each image URL
